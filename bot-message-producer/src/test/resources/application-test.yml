
server:
  port: ${SERVER_PORT:8080}

telegram:
  bot:
    need-registration: true

spring:
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:${DB_EXTERNAL_PORT:5432}/test
